# 仅用于 dbno=1112 的本地验证配置（同步/导入 + SceneTree 测试）

# 同步文件配置
total_sync = true
incr_sync = false
sync_history = false
enable_log = false
sync_graph_db = false
sync_versioned = false
sync_tidb = false
sync_live = false
replace_dbs = false
enable_index = false
sync_chunk_size = 10_0000

# 项目设置
project_path = "/Volumes/DPC/work/e3d_models"
included_projects = ["AvevaMarineSample"]

mdb_name = "ALL"
project_name = "AvevaMarineSample"
project_code = '1516'
surreal_ns = '1516'
module = "DESI"

# 只做导入/同步，不跑模型生成（避免耗时）
gen_model = false
gen_mesh = false
replace_mesh = false
save_db = true
apply_boolean_operation = true
mesh_tol_ratio = 3.0

# 房间/空间索引也先关掉
gen_spatial_tree = false
load_spatial_tree = false
save_spatial_tree_to_db = false
enable_sqlite_rtree = false
sqlite_index_path = "assets/aabb_index.sqlite"
gen_model_batch_size = 16

# Full Noun 模式关闭，让 manual_db_nums 生效
full_noun_mode = false
full_noun_max_concurrent_nouns = 2
full_noun_batch_size = 200
full_noun_enabled_categories = []
full_noun_excluded_nouns = []
debug_limit_per_noun = 0

build_cate_relate = false
meshes_path = "./assets/meshes"
gen_noun_graph = false

# arangodb 配置（不使用）
arangodb_database = "ams"
multi_threads = false
only_sync_sys = false
arangodb_url = "http://127.0.0.1:8529"
rebuild_ssc_tree = false
arangodb_user = "root"
arangodb_password = ""

# tidb 配置（不使用）
ip = "127.0.0.1"
user = "root"
password = ""
port = "3306"
sql_threads_number = 10
batch_insert_sql_cnt = 100

# surrealdb 数据库配置
v_ip = "localhost"
v_port = 8020
v_user = "root"
v_password = "root"
pe_chunk = 300
att_chunk = 200

kv_ip = "localhost"
kv_port = "8010"

mqtt_host = "127.0.0.1"
mqtt_port = 1883

location = "sjz"
location_dbs = [251181]

server_release_ip = "127.0.0.1:9099"

remote_file_server_hosts = []
file_server_host = "http://localhost:8082/assets/archives"

surreal_script_dir = "../rs-core/resource/surreal"

# 调试/导入目标
included_db_files = ["ams1112_0001"]
manual_db_nums = [1112]

room_keyword = "-RM"
plat_url = ""

gen_history_model = false
debug_model_refnos = []

# 调试选项（rs-core 的 DbOption 反序列化需要这些字段存在）
save_model_mesh_to_graph_db = true
need_sync_refno_basic = false
reset_mdb_project = false
only_update_dbinfo = false
debug_print_world_transform = false
only_rebuild_pdms_element = false
debug_refno_types = ["CATA", "LOOP", "PRIM"]

# 二号机组/额外配置（留空）
puhua_database_ip = ""
puhua_database_user = ""
puhua_database_password = ""

# 内存KV数据库配置（有默认值，但这里显式写出）
mem_kv_ip = "localhost"
mem_kv_port = "8011"
mem_kv_user = "root"
mem_kv_password = "root"

[mesh_precision]
default_lod = "L1"
