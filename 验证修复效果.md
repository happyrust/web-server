# 布尔运算实体方位显示修复验证指南

## 修复内容回顾

1. **export_common.rs**：
   - 在 `collect_export_data` 函数中添加了批量查询布尔运算结果的逻辑
   - 使用 `bool_set.contains(&geom_inst.refno)` 正确设置 `has_neg`

2. **export_prepack_lod.rs**：
   - 修复了两处 `refno_has_neg_map` 和 `instance_has_neg_map` 的设置
   - 同样使用批量查询确保准确性

## 验证步骤

### 1. 编译验证
```bash
cargo check --bin aios-database
```
应该没有编译错误。

### 2. 运行测试命令
```bash
cargo run --bin aios-database -- --debug-model 17496_106028 --regen-model --export-obj --verbose
```

### 3. 检查关键日志输出
运行时应该看到：
- `⚠️ 查询布尔运算结果失败: ...`（如果没有布尔结果）
- 或者正常输出，没有错误信息

### 4. 验证导出文件
检查生成的 `output/17496_106028.obj` 文件：
- 布尔运算的实体应该在正确的位置
- 不应该出现位置偏移或重复变换的问题

### 5. 对比测试
可以对比修复前后的导出结果：
- 修复前：布尔运算实体位置错误
- 修复后：布尔运算实体位置正确

## 预期结果

修复后，布尔运算实体的 `has_neg` 标识应该正确设置：
- 布尔结果：`has_neg = true`，使用 `local_transform`（已包含世界变换）
- 普通几何：`has_neg = false`，使用 `world_transform × local_transform`

## 故障排查

如果仍有问题，请检查：
1. 数据库中 `inst_relate_bool` 表是否有正确的记录
2. 查询 SQL 是否正确执行
3. 导出时的变换矩阵计算是否正确
